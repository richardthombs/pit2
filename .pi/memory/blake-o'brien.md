# Blake O'Brien — Memory

## Project context
- Working directory: /Users/richardthombs/dev/pit2
- Task tracker: `bd` (Beads). Actor flag required: `--actor "Blake O'Brien"`
- Bead lifecycle: claim on start (`--claim`), revert on failure, EM closes on success.

## Codebase conventions
- Agent role definitions live in `.pi/agents/*.md`
- Memory files live in `.pi/memory/<name>.md`
